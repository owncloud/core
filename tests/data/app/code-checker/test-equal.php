<?php

/**
 * Class BadClass - uses equal instead of identical operator
 */
class BadClass {
	/*
	public function foo() {
		if (true == false) {
		}
	}
	* I think it' the same case of the test-not-equal and test-identical-operator files
	*/
}
